name: CI — School Ops Responder

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

permissions:
  contents: read
  pull-requests: write

jobs:
  # Gate 1: GATES.md structural lint
  lint-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - name: Lint GATES.md
        run: node scripts/gate-lint.mjs GATES.md

  # Gate 2-4: Python seed + verification
  python-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Seed database
        run: python3 scripts/seed-database.py
      - name: Install Node dependencies for sqlite3
        run: npm install
      - name: Verify schema (raw SQL check)
        run: |
          node -e "
          const sqlite3 = require('sqlite3');
          const db = new sqlite3.Database('./school_ops.db');
          db.all(\"SELECT name FROM sqlite_master WHERE type='table'\", (_, rows) => {
            console.log('Tables:', rows.map(r => r.name).join(', '));
            db.close();
          });
          "
      - name: Verify anomalies
        run: node scripts/verify-anomalies.mjs

  # Gate 3: Node verification scripts
  node-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Install dependencies
        run: npm install
      - name: Seed database
        run: python3 scripts/seed-database.py
      - name: Verify seed
        run: node scripts/verify-seed.mjs
      - name: Verify anomalies
        run: node scripts/verify-anomalies.mjs
      - name: Verify agent spec
        run: node scripts/verify-agent.mjs

  # Qodo Code Review is provided by the Qodo GitHub App
  # (install via https://app.qodo.ai → Integrations → GitHub).
  # The .pr_agent.toml in this repo configures review behavior.
  # No GitHub Action needed — the App subscribes to PR events directly.

  # Build and verify all gates pass
  gate-check:
    runs-on: ubuntu-latest
    needs: [python-test, node-test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: npm install
      - name: Run all gate checks
        run: |
          python3 scripts/seed-database.py
          node scripts/verify-seed.mjs
          node scripts/verify-anomalies.mjs
          node scripts/verify-schema.mjs
          node scripts/verify-agent.mjs
          echo "All gates passed!"
